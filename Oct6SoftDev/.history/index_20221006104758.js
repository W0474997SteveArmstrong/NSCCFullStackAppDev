function removeDuplicates(arr){

    const arrResult = [];

    for (const iterator of object) {
        if(arrResult.includes(iterator)){
            console.log(iterator,"is already in result array.")
        }
        else{
            arrResult.push(iterator);
        }
    }
    return 
}

removeDuplicates([1,1,1,2,4,6,8,8,9]);